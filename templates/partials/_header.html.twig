<nav class="navbar">
    <div class="navbar-left">
       <img src="{{ asset('LogoEventify.png') }}" alt="Logo" class="logo-img">
       <!-- <h3 style="font-family: Quicksand;"> Eventify </h3> -->
    </div>

    <!-- Icône du menu burger -->
    <div class="burger" id="burger">
        <span></span>
        <span></span>
        <span></span>
    </div>

    <!-- Liens de navigation -->
    <ul class="nav-links" id="nav-links" style="font-family: Quicksand;">
        <li><a href="{{ path('app_events') }}">Voir tous les événements</a></li>

       {% if is_granted('ROLE_ADMIN') %}
    {# Menu "Listes" déroulant #}
    <li class="dropdown">
        <span class="dropbtn">Listes ▾</span>
        <ul class="dropdown-content">
            <li><a href="{{ path('listCategories') }}">Listes des Catégories</a></li>
            <li><a href="{{ path('listUsers') }}">Listes des Utilisateurs</a></li>
        </ul>
    </li>

    {# Menu "Créer" déroulant #}
    <li class="dropdown">
        <span class="dropbtn">Créer ▾</span>
        <ul class="dropdown-content">
            <li><a href="{{ path('createCategory') }}">Créer une Catégorie</a></li>
            <li><a href="{{ path('createmyevent') }}">Créer mon événement</a></li>
            <li><a href="{{ path('userAdmin_registration') }}">Créer un Utilisateur Admin</a></li>
        </ul>
    </li>

    {# Lien “Mes événements” et “Événements inscrits” pour admin aussi #}
    <li><a href="{{ path('myevents') }}">Mes événements</a></li>
    <li><a href="{{ path('registeredevents') }}">Événements Inscrits</a></li>
    <li><a href="{{ path('myprofile') }}">Mon profil</a></li>
    <li>
        <a href="{{ path('logout', {_csrf_token: csrf_token('logout')}) }}"
           onclick="event.preventDefault(); document.getElementById('logout-form').submit();">
           Déconnexion
        </a>
        <form id="logout-form" action="{{ path('logout') }}" method="POST" style="display:none;"></form>
    </li>

{% elseif app.user %}
    {# Utilisateur normal connecté #}
    <li><a href="{{ path('myevents') }}">Mes événements</a></li>
    <li><a href="{{ path('registeredevents') }}">Événements Inscrits</a></li>
    <li><a href="{{ path('createmyevent') }}">Créer mon événement</a></li>
    <li><a href="{{ path('myprofile') }}">Mon profil</a></li>
    <li>
        <a href="{{ path('logout', {_csrf_token: csrf_token('logout')}) }}"
           onclick="event.preventDefault(); document.getElementById('logout-form').submit();">
           Déconnexion
        </a>
        <form id="logout-form" action="{{ path('logout') }}" method="POST" style="display:none;"></form>
    </li>

{% else %}
    {# Non connecté #}
    <li><a href="{{ path('user_registration') }}">Inscription</a></li>
    <li><a href="{{ path('login') }}">Connexion</a></li>
{% endif %}


      <!--  <li><a href="#">Contact</a></li> -->
    </ul>


</nav>







  









    
<style>
/* --- NAVBAR --- */
.navbar {
    display: flex;
    justify-content: space-between;
    align-items: center;
    background: grey;
    color: white;
    padding: 10px 20px;
}

.logo-img {
    height: 120px;
    width: 100px;
    border-radius: 10px;
}

.logo {
    font-size: 1.5rem;
    font-weight: bold;
    margin-left: 10px;
}






/* --- Dropdown ADMIN --- */
.dropdown {
    position: relative; /* Nécessaire pour positionner le sous-menu */
}

.dropdown-content {
    display: none; /* On cache par défaut */
    padding: 0;
    position: absolute;
    top: 100%;
    background-color: #1d1f20;
    min-width: 60px;
    border-radius: 5px;
    overflow: hidden;
    z-index: 1000;
}

.dropdown-content li a {
    display: block;
    padding: 10px 15px;
    color: white;
    font-weight: normal;
    text-decoration: none;
}

.dropdown-content li a:hover {
    background-color: #005791;
}

/* Affiche le dropdown uniquement au survol du parent */
.dropdown:hover .dropdown-content {
    display: block;
    cursor: pointer;
}








/* --- LIENS NAV --- */
.nav-links {
    list-style: none;
    display: flex;
    
    justify-content: center;
}

.nav-links li {
    display: flex;
    justify-content: center;
}

.nav-links li a {
    display: flex;
    justify-content: center;
    color: white;
    text-decoration: none;
    font-weight: 500;
    transition: color 0.3s;
    justify-content: center;
    text-align: center;

}

.nav-links li a:hover {
    color: #00aaff;
}

/* --- MENU BURGER --- */
.burger {
    display: none;
    flex-direction: column;
    cursor: pointer;
    gap: 5px;
}


.burger span {
    width: 25px;
    height: 3px;
    background: white;
    border-radius: 3px;
    transition: all 0.3s;
}

/* --- ANIMATION BURGER --- */
.burger.open span:nth-child(1) {
    transform: rotate(45deg) translate(5px, 5px);
}
.burger.open span:nth-child(2) {
    opacity: 0;
}
.burger.open span:nth-child(3) {
    transform: rotate(-45deg) translate(5px, -5px);
}

/* --- RESPONSIVE --- */
@media (max-width: 768px) {
    .burger {
        display: flex;
    }

    .nav-links {
        display: none;
        width: 100%;
        background: #1d1f20;
        position: absolute;
        top: 150px;
        left: 0;
        padding: 20px 0;
        text-align: center;
        border-top: 1px solid #333;
        z-index: 1000;
    }

    .nav-links.active {
        display: flex;
        animation: slideDown 0.3s ease forwards;
    }

    @keyframes slideDown {
        from { opacity: 0; transform: translateY(-10px); }
        to { opacity: 1; transform: translateY(0); }
    }

    .nav-links li {
        margin: 10px 0;
    }

    
}









@media (max-width: 700px) {

    .burger {
        display: flex;
    }

    .nav-links {
        display: none;
        flex-direction: column;   /* ⬅️ LIGNE CLÉ */
        width: 100%;
        background: #1d1f20;
        position: absolute;
        top: 150px;
        left: 0;
        text-align: center;
        border-top: 1px solid #333;
        z-index: 1000;
    }

    .nav-links.active {
        display: flex;
        animation: slideDown 0.3s ease forwards;
    }

    

    .nav-links li {
        width: 100%;
        display: flex;
        justify-content: center;
        margin: 0;
    }

    .nav-links li a,
    .dropbtn {
        width: 100%;
        text-align: center;
    }
}



</style>
