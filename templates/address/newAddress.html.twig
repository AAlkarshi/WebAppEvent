{# templates/address/new.html.twig #}
{% extends 'base.html.twig' %}

{% block title %}Nouvelle Adresse{% endblock %}

{% block body %}
<h1>Ajouter une Adresse</h1>

{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary">Enregistrer</button>
{{ form_end(form) }}

<a href="{{ path('address_index') }}">Retour à la liste des adresses</a>
{% endblock %}
